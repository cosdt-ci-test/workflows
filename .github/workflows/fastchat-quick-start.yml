# fastchat quick start guard - project thin trigger.
#
# Calls the common engine .github/workflows/quick-start-template.yml;
# this file only declares what varies per project: schedule,
# concurrency, runner, container image + options, upstream repo,
# monitored doc, and the test entry command. The guard loop, cache
# I/O, state relay and publishing all live in the engine. See the
# engine's `workflow_call.inputs` block for the full input contract.
#
# Engine-fixed behaviours this trigger relies on (declared in the
# engine, NOT here):
#   * cache key prefix: quick-start-monitor-state-fastchat_  (from inputs.project)
#   * artifact name:    fastchat-quick-start-<run_id>
#   * result.json is written by the engine and validated against
#     schemas/result.schema.json before upload.
#
# The schedule runs every six hours at minute 45. workflow_dispatch
# remains available for branch validation and manual reruns.
#
# Bind mount: the CLI and model worker download real model weights from
# ModelScope, so the ModelScope cache is
# bind-mounted to the host (/data/ci-cache/modelscope/fastchat) to keep
# the downloaded weights persistent across runs. The self-hosted NPU
# runner (linux-aarch64-a2-1) auto-passes /dev/davinci* into the
# container via the cluster device-plugin.

name: fastchat-quick-start

concurrency:
  # format() is load-bearing: a '||' between 'manual-' and
  # github.run_id would short-circuit on the truthy literal and
  # every dispatch would share one 'manual-' group.
  # Scheduled runs share one group; dispatch runs use their run ID.
  group: ${{ github.event_name == 'schedule' && 'fastchat-quick-start-schedule' || format('manual-{0}', github.run_id) }}
  # cancel-in-progress: false because (1) a schedule run cancelled
  # mid-way loses its outcome writeback - the outcome is what makes
  # the retry mechanism work, and the 'if: always()' guard isn't
  # enough when the container is being torn down; (2) dispatch runs
  # already live in unique groups so there's nothing to cancel.
  cancel-in-progress: false

on:
  schedule:
    - cron: '45 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fastchat-quick-start:
    uses: ./.github/workflows/quick-start-template.yml
    with:
      # Namespaces cache keys (quick-start-monitor-state-fastchat_*), artifacts
      # (fastchat-quick-start-<run_id>) and the test working dir
      # (workflows/projects/fastchat).
      project: fastchat
      # Self-hosted NPU runner for the test job only; the engine pins
      # the cache I/O jobs (restore-cache / publish-and-persist) to
      # GitHub-hosted ubuntu-latest.
      test_runner: '["linux-aarch64-a2-1"]'
      image: swr.cn-south-1.myhuaweicloud.com/ascendhub/cann:9.1.0-910b-ubuntu22.04-py3.12
      container_options: >-
        --volume=/data/ci-cache/modelscope/fastchat:/root/.cache/modelscope
      # Doc flow downloads real weights via FASTCHAT_USE_MODELSCOPE=True;
      # the bind-mount above persists the ModelScope cache on the host.
      timeout_minutes: 120
      upstream_repo: lm-sys/FastChat
      # Contents API + the triggering SHA keeps branch dispatches pinned to
      # the document from the same commit. The test fetches this URL through
      # the shared MarkdownDocTestBase implementation.
      doc_url: https://api.github.com/repos/cosdt-ci-test/workflows/contents/projects/fastchat/docs/Quick-start-Ascend.md?ref=${{ github.sha }}
      doc_path: https://github.com/cosdt-ci-test/workflows/blob/main/projects/fastchat/docs/Quick-start-Ascend.md
      # cwd is workflows/projects/fastchat; env contract (MONITORED_DOC_URL /
      # NPU_READY) is injected by the engine. All project env prep and
      # installs live in the test subclass's prepare_environment hook.
      test_command: python -m unittest tests.test_quick_start_ascend -v 2>&1
