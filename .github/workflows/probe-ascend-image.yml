name: probe-ascend-image

on:
  workflow_dispatch:

jobs:
  probe-torch-npu:
    runs-on: [linux-aarch64-a2-2]
    timeout-minutes: 60
    defaults:
      run:
        shell: bash
    container:
      image: swr.cn-south-1.myhuaweicloud.com/ascendhub/torch-npu:2.9.0.post2-910b-ubuntu22.04-py3.11
      options: >-
        --privileged --shm-size=64g
        --device=/dev/davinci0 --device=/dev/davinci1
        --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc
        --volume=/usr/local/Ascend/driver:/usr/local/Ascend/driver:ro
        --volume=/usr/local/sbin/npu-smi:/usr/local/sbin/npu-smi:ro
        --volume=/etc/ascend_install.info:/etc/ascend_install.info:ro
        --volume=/data/ci-cache/modelscope:/root/.cache/modelscope
    env:
      MODELSCOPE_CACHE: /root/.cache/modelscope
      ASCEND_RT_VISIBLE_DEVICES: "0,1"
    steps:
      - name: Check NPU, torch_npu, and cache
        run: |
          set -euo pipefail
          export PATH="/usr/local/sbin:/usr/local/bin:$PATH"
          if [ -f /usr/local/Ascend/ascend-toolkit/set_env.sh ]; then
            set +u
            # shellcheck disable=SC1091
            source /usr/local/Ascend/ascend-toolkit/set_env.sh
            set -u
          fi
          npu-smi info
          python -c "import torch, torch_npu; print(torch.__version__); print(torch_npu.__version__); print(torch.npu.device_count())"
          mkdir -p /root/.cache/modelscope
          echo probe > /root/.cache/modelscope/.write-probe
          test -s /root/.cache/modelscope/.write-probe
          rm -f /root/.cache/modelscope/.write-probe
          echo "torch-npu image locked"

  probe-cann:
    needs: probe-torch-npu
    if: ${{ failure() }}
    runs-on: [linux-aarch64-a2-2]
    timeout-minutes: 90
    defaults:
      run:
        shell: bash
    container:
      image: swr.cn-south-1.myhuaweicloud.com/ascendhub/cann:9.0.0-910b-ubuntu22.04-py3.11
      options: >-
        --privileged --shm-size=64g
        --device=/dev/davinci0 --device=/dev/davinci1
        --device=/dev/davinci_manager --device=/dev/devmm_svm --device=/dev/hisi_hdc
        --volume=/usr/local/Ascend/driver:/usr/local/Ascend/driver:ro
        --volume=/usr/local/sbin/npu-smi:/usr/local/sbin/npu-smi:ro
        --volume=/etc/ascend_install.info:/etc/ascend_install.info:ro
        --volume=/data/ci-cache/modelscope:/root/.cache/modelscope
    env:
      MODELSCOPE_CACHE: /root/.cache/modelscope
      ASCEND_RT_VISIBLE_DEVICES: "0,1"
    steps:
      - name: Check NPU, install torch_npu, and cache
        run: |
          set -euo pipefail
          export PATH="/usr/local/sbin:/usr/local/bin:$PATH"
          if [ -f /usr/local/Ascend/ascend-toolkit/set_env.sh ]; then
            set +u
            # shellcheck disable=SC1091
            source /usr/local/Ascend/ascend-toolkit/set_env.sh
            set -u
          fi
          npu-smi info
          python -m pip install -U pip
          python -m pip install torch==2.9.0 torch_npu==2.9.0.post2 \
            --index-url https://pypi.org/simple \
            --extra-index-url https://repo.huaweicloud.com/ascend/repos/pypi
          python -c "import torch, torch_npu; print(torch.__version__); print(torch_npu.__version__); print(torch.npu.device_count())"
          mkdir -p /root/.cache/modelscope
          echo probe > /root/.cache/modelscope/.write-probe
          test -s /root/.cache/modelscope/.write-probe
          rm -f /root/.cache/modelscope/.write-probe
          echo "cann image locked; install torch at job start"
