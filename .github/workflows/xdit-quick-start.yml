# xDiT quick start guard - project thin trigger.
#
# Calls the common engine .github/workflows/quick-start-template.yml;
# this file only declares what varies per project: schedule,
# concurrency, runner, container image + options, upstream repo,
# monitored doc, and the test entry command. The guard loop, cache
# I/O, state relay and publishing all live in the engine. See the
# engine's `workflow_call.inputs` block for the full input contract.
#
# Cache keys (monitor-state-xdit-*) and artifact names are derived
# from project=xdit, not from the workflow name.
#
# Container options:
#   * No explicit device mounts: the self-hosted NPU runner
#     (`linux-aarch64-a2-2`, two 910B4) auto-passes `/dev/davinci*`
#     into the container via the cluster device-plugin.
#   * ModelScope cache bind-mount: the doc's `#test-setup
#     store="model_path"` step runs `snapshot_download
#     ('stabilityai/stable-diffusion-3-medium-diffusers', ...)`, which
#     pulls ~4 GB of bf16 weights into `~/.cache/modelscope/.../`.
#     Bind-mounting a host-side persistent cache (`/data/ci-cache/
#     modelscope/xdit`) keeps that download off the run's wall-clock
#     budget across runs; without it the 6-hour schedule would
#     re-download the same ~4 GB on every fire. Same `<project>`
#     token used by ms-swift / diffusers / xtuner / specforge.

name: xdit-quick-start

concurrency:
  # format() is load-bearing: a '||' between 'manual-' and
  # github.run_id would short-circuit on the truthy literal and
  # every dispatch would share one 'manual-' group.
  group: ${{ github.event_name == 'schedule' && 'xdit-quick-start-schedule' || format('manual-{0}', github.run_id) }}
  # cancel-in-progress: false because (1) a schedule run cancelled
  # mid-way loses its outcome writeback - the outcome is what makes
  # the retry mechanism work, and the 'if: always()' guard isn't
  # enough when the container is being torn down; (2) dispatch runs
  # already live in unique groups so there's nothing to cancel.
  cancel-in-progress: false

on:
  schedule:
    # Run every 6 hours: '0 */6 * * *' fires at minute 0 of every
    # 6th hour (00:00, 06:00, 12:00, 18:00). Cadence is loose enough
    # for an upstream that releases a few times a year (xDiT current
    # stable line is 0.4.x: 0.4.5 was the most recent stable on
    # 2026-08; 0.4.6rc1 is in flight), but tight enough that doc
    # fixes propagate within a day.
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  xdit-quick-start:
    uses: ./.github/workflows/quick-start-template.yml
    with:
      # Namespaces cache keys (monitor-state-xdit-*), artifacts
      # (xdit-quick-start-<run_id>) and the test working dir
      # (workflows/projects/xdit).
      project: xdit
      # Self-hosted NPU runner for the test job only; the engine
      # pins the cache I/O jobs (restore-cache / publish-and-persist)
      # to GitHub-hosted ubuntu-latest. 2-card label is required
      # because xDiT's PR #566 NPU support is single-node multi-card
      # (USP / DP / CFG-parallel on xfuser, no PipeFusion), and the
      # doc exercises that path with `xdit --ulysses_degree 2
      # --ring_degree 1 ...` which `xfuser.cli.get_nproc_from_args`
      # auto-expands into `torchrun --nproc_per_node=2 -m
      # xfuser.runner ...`. Single-card (`a2-1`) cannot init the
      # HCCL distributed process group with `--nproc_per_node=2`.
      test_runner: '["linux-aarch64-a2-2"]'
      image: swr.cn-south-1.myhuaweicloud.com/ascendhub/cann:9.1.0-910b-ubuntu22.04-py3.12
      # ModelScope cache bind-mount: persistent host-side cache at
      # `/data/ci-cache/modelscope/xdit` is reused across runs so the
      # `snapshot_download('stabilityai/stable-diffusion-3-medium-
      # diffusers')` step in the doc only fetches weights once.
      # `:z` is load-bearing on this SELinux-enforcing cluster; see
      # ms-swift / diffusers / xtuner / specforge for the same
      # option. The mount point inside the container is
      # `/root/.cache/modelscope`, which is where the modelscope
      # SDK resolves its cache by default — `MODELSCOPE_CACHE` env
      # is not required.
      container_options: >-
        --volume=/data/ci-cache/modelscope/xdit:/root/.cache/modelscope:z
      # Doc body does: torch / torch_npu install + verify + diffusers /
      # transformers / accelerate install + git clone (depth=1, ref =
      # upstream release tag) + `uv pip install -e . --no-deps` (xfuser
      # 0.4.x setup.py) + `uv pip install yunchang distvae av` (the
      # deps skipped by --no-deps that NPU smoke actually needs) +
      # `xdit --help` parse + the `xfuser-detect-npu` import smoke
      # (verifies xfuser/envs.py's _is_npu() / hccl dispatch) +
      # `snapshot_download(stable-diffusion-3-medium-diffusers)` to
      # stage SD3 medium into the bind-mounted ModelScope cache +
      # single-card `xdit --ulysses_degree 1 --ring_degree 1 ...`
      # inference + multi-card `xdit --ulysses_degree 2
      # --ring_degree 1 ...` inference (xfuser.cli auto-launches
      # `torchrun --nproc_per_node=2`). `--no-deps` here is
      # load-bearing: xfuser setup.py's `install_requires` includes
      # `accelerate>=0.33.0`, `transformers>=4.39.1`, `diffusers>=0.33.0`
      # which on aarch64 NPU pulls nvidia-* CUDA wheels transitively
      # via cluster-cache resolution; we install those ourselves in
      # the doc's ## 前置安装 with PIP_CONSTRAINT= blocking CUDA,
      # matching upstream ascend_npu.md's pattern.
      # 90 min covers cold install + first git clone + the
      # build-from-source `pip install -e . --no-deps` step +
      # SD3 medium cold-cache download (~4 GB; warm after first run
      # via the bind-mount) + the always-on import / CLI smoke +
      # single-card inference (~30s warm) + multi-card inference
      # (~30-60s warm; HCCL init adds ~10-15s on the first run).
      timeout_minutes: 90
      upstream_repo: xdit-project/xDiT
      # GitHub Contents API URL (NOT raw.githubusercontent.com):
      #   * Engine's monitor hashes via `Authorization: Bearer
      #     $GH_TOKEN` + `Accept: application/vnd.github.raw`, which
      #     is the Contents API contract for raw markdown. raw URLs
      #     ignore those headers (different auth surface), so the
      #     engine would hash an unauthenticated blob and miss doc
      #     edits. API URL is the only form where the monitor + test-
      #     side fetches both line up with the same Authorization +
      #     Accept.
      #   * `markdown_doc_test_base.py` already special-cases
      #     `api.github.com` URLs and re-applies the same Accept
      #     header on the test-side fetch (MONITORED_DOC_URL), so
      #     the test inherits the same authenticated contract.
      # `?ref=${{ github.sha }}` pins the doc fetch to whichever
      # ref fired the run (branch tip on `pull_request`, merge commit
      # on `push`, default branch tip on `schedule`); without it
      # dispatch from a non-default branch fetches `main` and branch-
      # side doc edits can't be tested. Same pattern as xtuner /
      # specforge / aibrix / colossalai.
      doc_url: https://api.github.com/repos/cosdt-ci-test/workflows/contents/projects/xdit/docs/Quick-start-Ascend.md?ref=${{ github.sha }}
      doc_path: https://github.com/cosdt-ci-test/workflows/blob/${{ github.sha }}/projects/xdit/docs/Quick-start-Ascend.md
      # cwd is workflows/projects/xdit; env contract (MONITORED_DOC_URL /
      # UPSTREAM_REF / NPU_READY) is injected by the engine. All project
      # env prep and installs live in the test subclass's
      # prepare_environment hook.
      test_command: python -m unittest tests.test_quick_start_ascend -v 2>&1
