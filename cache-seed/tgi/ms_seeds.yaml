# TGI quick-start 例程资产的 ModelScope plant spec（由 cache-seed workflow 的
# scripts/ms_seed.py 执行）。setup 不下载模型：Quick-start-Ascend.md 的
# 「2. 下载基础模型」用 modelscope snapshot_download 命中本地缓存，
# 经 store="model_path" 注入后续 #test 块。
version: 1
seeds:
  # Quick-start-Ascend.md「2. 下载基础模型」硬编码的模型：
  # snapshot_download('Qwen/Qwen3-0.6B')，/generate 冒烟测试的底座
  - ms_id: Qwen/Qwen3-0.6B
    hf_id: Qwen/Qwen3-0.6B
    kind: model
